import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fly me to the Mars",
  description:
    "An immersive 3D interactive experience that takes you on an epic journey from Earth to Mars. Explore the red planet through realistic 3D visualizations, discover fascinating stories about Martian features like Olympus Mons and Valles Marineris, and navigate the 2D/3D map with intelligent AI search. An educational space adventure built with Three.js and Next.js.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
